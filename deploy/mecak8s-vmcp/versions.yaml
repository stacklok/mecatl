# Pinned external runtimes for the disposable vMCP integration fixture.
# Runtime image IDs are captured from the running Pods; do not infer them from
# mecatl's ToolHive Go-module dependency.
certManager:
  chartVersion: "v1.17.2"
  chart: "oci://quay.io/jetstack/charts/cert-manager"
toolhive:
  chartVersion: "0.45.0"
  sourceCommit: "cc922a8b47652988ae4d057a957942385fc59270"
  operatorImage: "ghcr.io/stacklok/toolhive/operator:v0.45.0"
  vmcpImage: "ghcr.io/stacklok/toolhive/vmcp:v0.45.0"
keycloak:
  # Fixture IdP for the external-issuer token-exchange path. Unlike Dex, Keycloak
  # emits `scope` on access tokens and can shape `aud` via an audience mapper.
  version: "26.7.2"
  image: "quay.io/keycloak/keycloak:26.7.2"
yardstick:
  version: "1.1.1"
  sourceCommit: "e5b8908ed6f53c1171ac805d82cf858d2982fa19e"
  image: "ghcr.io/stackloklabs/yardstick/yardstick-server:1.1.1"
