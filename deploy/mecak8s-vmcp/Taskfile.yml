version: '3'

# Historical ToolHive vMCP manifests remain here as reference material. The
# operator-run mecak8s Kind lifecycle moved to deploy/mecak8s-kind/.
tasks:
  vmcp-check:
    desc: Validate the historical vMCP integration manifests without creating a cluster
    cmds:
      - go test ./internal/deploycheck -run 'Test(VMCPFixture|Mecak8sVMCPPOC_)'
