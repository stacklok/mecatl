version: '3'

# Disposable, operator-run mecak8s Kind baseline. It intentionally owns only
# Kind, the local chart/image, and in-chart local Redis.
vars:
  CLUSTER: mecatl-dev
  CONTEXT: kind-mecatl-dev
  NAMESPACE: mecatl
  RELEASE: mecak8s
  STATE: .scratch/kind/mecatl-dev
  KUBECONFIG: deploy/mecak8s-kind/kconfig.yaml
  SETUP_LOCK: .scratch/kind/mecatl-dev.setup-lock
  IMAGE: ko.local/mecak8s:dev
  PROVIDER_SECRET: mecak8s-openrouter

tasks:
  kind-setup:
    desc: Recreate the local mecak8s Kind baseline with local Redis
    cmds:
      - task: reset-state
      - task: cluster-create
      - task: namespace-apply
      - task: image-build-load
      - task: chart-apply

  kind-keycloak-apply:
    desc: Idempotently apply the optional Keycloak, certificate, and OIDC/TLS layer
    cmds:
      - task: cert-manager-install
      - task: certificate-apply
      - task: keycloak-apply
      - task: chart-keycloak-apply

  kind-keycloak-setup:
    desc: Recreate the Kind baseline then apply its optional Keycloak identity layer
    cmds:
      - task: kind-setup
      - task: kind-keycloak-apply

  kind-keycloak-demo:
    desc: Supervise loopback forwards and print the remote OIDC quickstart (requires kind-keycloak-setup and kind-hosts-add)
    preconditions:
      - sh: command -v nc >/dev/null 2>&1
        msg: nc is required to wait for the loopback forwards
    cmds:
      - task: cluster-ready
      - cmd: |
          set -eu
          state={{.STATE}}
          ca="$state/fixture-ca.crt"
          keycloak_log="$state/keycloak-port-forward.log"
          mecak8s_log="$state/mecak8s-port-forward.log"
          encoded="$state/fixture-ca.b64.$$"
          decoded="$ca.tmp.$$"
          mkdir -p "$state"

          cleanup() {
            status=$?
            trap - EXIT INT TERM
            [ -z "${keycloak_pid:-}" ] || kill "$keycloak_pid" 2>/dev/null || true
            [ -z "${mecak8s_pid:-}" ] || kill "$mecak8s_pid" 2>/dev/null || true
            [ -z "${keycloak_pid:-}" ] || wait "$keycloak_pid" 2>/dev/null || true
            [ -z "${mecak8s_pid:-}" ] || wait "$mecak8s_pid" 2>/dev/null || true
            rm -f "$encoded" "$decoded"
            exit "$status"
          }
          trap cleanup EXIT INT TERM

          kubectl --kubeconfig={{.KUBECONFIG}} --context={{.CONTEXT}} --namespace={{.NAMESPACE}} port-forward --address=127.0.0.1 svc/keycloak 8443:8443 >"$keycloak_log" 2>&1 &
          keycloak_pid=$!
          kubectl --kubeconfig={{.KUBECONFIG}} --context={{.CONTEXT}} --namespace={{.NAMESPACE}} port-forward --address=127.0.0.1 svc/{{.RELEASE}}-mecak8s 18080:8080 18081:8081 >"$mecak8s_log" 2>&1 &
          mecak8s_pid=$!

          wait_port() {
            name=$1
            port=$2
            pid=$3
            log=$4
            attempt=0
            while [ "$attempt" -lt 30 ]; do
              if nc -z 127.0.0.1 "$port"; then
                return 0
              fi
              if ! kill -0 "$pid" 2>/dev/null; then
                echo "$name port-forward exited before accepting connections; log: $log" >&2
                cat "$log" >&2 || true
                return 1
              fi
              attempt=$((attempt + 1))
              sleep 1
            done
            echo "$name port-forward did not accept loopback connections; log: $log" >&2
            cat "$log" >&2 || true
            return 1
          }

          wait_port Keycloak 8443 "$keycloak_pid" "$keycloak_log"
          wait_port mecak8s-gRPC 18080 "$mecak8s_pid" "$mecak8s_log"
          wait_port mecak8s-HTTPS 18081 "$mecak8s_pid" "$mecak8s_log"
          kubectl --kubeconfig={{.KUBECONFIG}} --context={{.CONTEXT}} --namespace={{.NAMESPACE}} get secret fixture-ca -o jsonpath='{.data.tls\.crt}' >"$encoded"
          if base64 -d "$encoded" >"$decoded" 2>/dev/null; then
            :
          elif base64 -D "$encoded" >"$decoded" 2>/dev/null; then
            :
          else
            echo "failed to decode fixture CA" >&2
            exit 1
          fi
          rm -f "$encoded"
          chmod 0600 "$decoded"
          mv "$decoded" "$ca"

          printf '\nFixture forwards are ready. In another terminal, run:\n\n'
          printf '%s\n' "mecatui login localhost:18081 --issuer https://keycloak.mecatl.svc.cluster.local:8443/realms/mecatl --client-id mecatui-kind --audience mecak8s --tls-ca $ca --scopes openid,profile,mecak8s:access,offline_access"
          printf '%s\n' "mecatui connect localhost:18081 --tls --tls-ca $ca"
          printf '\nThe CA is at %s. This task keeps both loopback forwards alive; press Ctrl-C here to stop them.\n' "$ca"

          while kill -0 "$keycloak_pid" 2>/dev/null && kill -0 "$mecak8s_pid" 2>/dev/null; do
            sleep 1
          done
          echo "a fixture port-forward exited; stopping the remaining forward" >&2
          exit 1

  kind-status:
    desc: Show the local mecak8s Kind baseline
    cmds:
      - task: cluster-ready
      - kubectl --kubeconfig={{.KUBECONFIG}} --context={{.CONTEXT}} get pods,services,statefulsets -n {{.NAMESPACE}}
      - helm status {{.RELEASE}} --namespace={{.NAMESPACE}} --kubeconfig={{.KUBECONFIG}} --kube-context={{.CONTEXT}}

  kind-port-forward:
    desc: Forward mecak8s gRPC and HTTP to loopback only (Ctrl-C to stop)
    cmds:
      - task: cluster-ready
      - kubectl --kubeconfig={{.KUBECONFIG}} --context={{.CONTEXT}} --namespace={{.NAMESPACE}} port-forward --address=127.0.0.1 svc/{{.RELEASE}}-mecak8s 18080:8080 18081:8081

  kind-keycloak-port-forward:
    desc: Forward Keycloak HTTPS to loopback only (Ctrl-C to stop)
    cmds:
      - task: cluster-ready
      - kubectl --kubeconfig={{.KUBECONFIG}} --context={{.CONTEXT}} --namespace={{.NAMESPACE}} port-forward --address=127.0.0.1 svc/keycloak 8443:8443

  kind-hosts-show:
    desc: Show the temporary loopback alias required for the Keycloak issuer
    cmds:
      - cmd: printf '%s\n' '127.0.0.1 keycloak.mecatl.svc.cluster.local'

  kind-hosts-add:
    desc: Add the temporary Keycloak issuer alias to /etc/hosts (loopback only)
    cmds:
      - cmd: |
          set -eu
          entry='127.0.0.1 keycloak.mecatl.svc.cluster.local'
          sudo sh -c 'grep -Fqx "$1" /etc/hosts || printf "%s\\n" "$1" >> /etc/hosts' sh "$entry"
          echo 'Added (or already present):'
          task mecak8s:kind-hosts-show

  kind-hosts-remove:
    desc: Remove only the temporary Keycloak issuer alias from /etc/hosts
    cmds:
      - cmd: |
          set -eu
          sudo sed -i.bak -e '\|^127\.0\.0\.1 keycloak\.mecatl\.svc\.cluster\.local$|d' /etc/hosts
          echo 'Removed exact temporary entry (backup: /etc/hosts.bak).'

  kind-destroy:
    desc: Delete the named Kind baseline and its local state
    cmds:
      - kind delete cluster --name={{.CLUSTER}}
      - rm -rf {{.STATE}}
      - rm -f {{.KUBECONFIG}} {{.SETUP_LOCK}}

  reset-state:
    internal: true
    preconditions:
      - sh: command -v kind >/dev/null 2>&1
        msg: kind is required
    cmds:
      - cmd: |
          set -eu
          kind delete cluster --name={{.CLUSTER}} 2>/dev/null || true
          rm -rf {{.STATE}}
          rm -f {{.KUBECONFIG}} {{.SETUP_LOCK}}
          mkdir -p .scratch/kind deploy/mecak8s-kind
          : > {{.SETUP_LOCK}}

  cluster-ready:
    internal: true
    preconditions:
      - sh: command -v kind >/dev/null 2>&1
        msg: kind is required
      - sh: command -v kubectl >/dev/null 2>&1
        msg: kubectl is required
    cmds:
      - cmd: |
          set -eu
          kind get clusters 2>/dev/null | grep -Fxq '{{.CLUSTER}}' || { echo "cluster {{.CLUSTER}} not found"; exit 1; }
          [ -f {{.KUBECONFIG}} ] || { echo "kubeconfig {{.KUBECONFIG}} not found"; exit 1; }
          [ "$(kubectl --kubeconfig={{.KUBECONFIG}} config current-context)" = '{{.CONTEXT}}' ] || { echo "kubeconfig context mismatch"; exit 1; }

  cluster-create:
    internal: true
    preconditions:
      - sh: command -v kubectl >/dev/null 2>&1
        msg: kubectl is required
    cmds:
      - cmd: |
          set -eu
          trap 'status=$?; deploy/mecak8s-kind/cleanup-on-failure.sh "$status"' EXIT
          mkdir -p {{.STATE}}
          kind create cluster --name={{.CLUSTER}} --config=deploy/mecak8s-kind/kind-config.yaml
          kind get kubeconfig --name={{.CLUSTER}} > {{.KUBECONFIG}}

  namespace-apply:
    internal: true
    preconditions:
      - sh: command -v kubectl >/dev/null 2>&1
        msg: kubectl is required
    cmds:
      - task: cluster-ready
      - kubectl --kubeconfig={{.KUBECONFIG}} --context={{.CONTEXT}} create namespace {{.NAMESPACE}} --dry-run=client -o yaml | kubectl --kubeconfig={{.KUBECONFIG}} --context={{.CONTEXT}} apply -f -
      - kubectl --kubeconfig={{.KUBECONFIG}} --context={{.CONTEXT}} label namespace {{.NAMESPACE}} pod-security.kubernetes.io/enforce=restricted pod-security.kubernetes.io/audit=restricted pod-security.kubernetes.io/warn=restricted --overwrite

  image-build-load:
    internal: true
    preconditions:
      - sh: command -v ko >/dev/null 2>&1
        msg: ko is required
    cmds:
      - task: cluster-ready
      - cmd: |
          set -eu
          trap 'status=$?; deploy/mecak8s-kind/cleanup-on-failure.sh "$status"' EXIT
          runtime=docker
          [ "${KIND_EXPERIMENTAL_PROVIDER:-}" != podman ] || runtime=podman
          command -v "$runtime" >/dev/null 2>&1 || { echo "$runtime is required"; exit 1; }
          KO_DOCKER_REPO=ko.local/mecak8s ko build --local --bare --tags=dev ./cmd/mecak8s >/dev/null
          archive={{.STATE}}/mecak8s.tar
          "$runtime" save {{.IMAGE}} -o "$archive"
          kind load image-archive "$archive" --name={{.CLUSTER}}
          rm -f "$archive"

  cert-manager-install:
    internal: true
    preconditions:
      - sh: command -v helm >/dev/null 2>&1
        msg: helm is required
    cmds:
      - task: cluster-ready
      - helm upgrade --install cert-manager oci://quay.io/jetstack/charts/cert-manager --version=v1.17.2 --namespace=cert-manager --create-namespace --set crds.enabled=true --kubeconfig={{.KUBECONFIG}} --kube-context={{.CONTEXT}} --wait --timeout=4m

  certificate-apply:
    internal: true
    preconditions:
      - sh: command -v kubectl >/dev/null 2>&1
        msg: kubectl is required
    cmds:
      - task: cluster-ready
      - kubectl --kubeconfig={{.KUBECONFIG}} --context={{.CONTEXT}} --namespace={{.NAMESPACE}} apply -f deploy/mecak8s-kind/fixture-tls.yaml
      - kubectl --kubeconfig={{.KUBECONFIG}} --context={{.CONTEXT}} --namespace={{.NAMESPACE}} wait --for=condition=Ready certificate/fixture-ca --timeout=120s
      - kubectl --kubeconfig={{.KUBECONFIG}} --context={{.CONTEXT}} --namespace={{.NAMESPACE}} wait --for=condition=Ready certificate/mecak8s-tls --timeout=120s
      - kubectl --kubeconfig={{.KUBECONFIG}} --context={{.CONTEXT}} --namespace={{.NAMESPACE}} wait --for=condition=Ready certificate/keycloak-tls --timeout=120s

  keycloak-apply:
    internal: true
    preconditions:
      - sh: command -v kubectl >/dev/null 2>&1
        msg: kubectl is required
    cmds:
      - task: cluster-ready
      - kubectl --kubeconfig={{.KUBECONFIG}} --context={{.CONTEXT}} --namespace={{.NAMESPACE}} apply -f deploy/mecak8s-kind/keycloak.yaml
      - kubectl --kubeconfig={{.KUBECONFIG}} --context={{.CONTEXT}} rollout status deployment/keycloak -n {{.NAMESPACE}} --timeout=240s

  chart-keycloak-apply:
    internal: true
    preconditions:
      - sh: command -v helm >/dev/null 2>&1
        msg: helm is required
    cmds:
      - task: cluster-ready
      - helm upgrade --install {{.RELEASE}} deploy/helm/mecak8s --namespace={{.NAMESPACE}} --kubeconfig={{.KUBECONFIG}} --kube-context={{.CONTEXT}} --values=deploy/helm/mecak8s/values-kind.yaml --values=deploy/helm/mecak8s/values-kind-keycloak.yaml --wait --timeout=4m
      - kubectl --kubeconfig={{.KUBECONFIG}} --context={{.CONTEXT}} rollout status deployment/{{.RELEASE}}-mecak8s -n {{.NAMESPACE}} --timeout=240s

  chart-apply:
    internal: true
    preconditions:
      - sh: command -v helm >/dev/null 2>&1
        msg: helm is required
    cmds:
      - task: cluster-ready
      - cmd: |
          set -eu
          trap 'status=$?; deploy/mecak8s-kind/cleanup-on-failure.sh "$status"' EXIT
          if [ -n "${OPENROUTER_API_KEY:-}" ]; then
            printf '%s' "$OPENROUTER_API_KEY" | kubectl --kubeconfig={{.KUBECONFIG}} --context={{.CONTEXT}} --namespace={{.NAMESPACE}} create secret generic {{.PROVIDER_SECRET}} --from-file=OPENROUTER_API_KEY=/dev/stdin --dry-run=client -o yaml | kubectl --kubeconfig={{.KUBECONFIG}} --context={{.CONTEXT}} apply -f -
            provider_values=deploy/mecak8s-kind/kind-provider-real.yaml
          else
            kubectl --kubeconfig={{.KUBECONFIG}} --context={{.CONTEXT}} delete secret {{.PROVIDER_SECRET}} --namespace={{.NAMESPACE}} --ignore-not-found
            provider_values=deploy/mecak8s-kind/kind-provider-mock.yaml
          fi
          helm upgrade --install {{.RELEASE}} deploy/helm/mecak8s --namespace={{.NAMESPACE}} --kubeconfig={{.KUBECONFIG}} --kube-context={{.CONTEXT}} --values=deploy/helm/mecak8s/values-kind.yaml --values="$provider_values" --wait --timeout=4m
          kubectl --kubeconfig={{.KUBECONFIG}} --context={{.CONTEXT}} rollout status statefulset/redis -n {{.NAMESPACE}} --timeout=240s
          kubectl --kubeconfig={{.KUBECONFIG}} --context={{.CONTEXT}} rollout status deployment/{{.RELEASE}}-mecak8s -n {{.NAMESPACE}} --timeout=240s
