# Session MCP authorization reconstruction review ledger

- Review fixed point: `5ed0e242f9a7c89e8897297b36512e4ece4fd7bd`
- Post-review reconciliation: `935f74713d351617bf41e0c578443454b153dd49`
- Comparison base: `e49d183d0`
- Panel: 13 read-only `gpt-5.6-terra` reviews
- Scope: 31 commits, 136 files, +23,225/-4,101

This is the durable work queue for the reconstructed branch. `known` means the
required behavior and implementation direction are understood; it does not
mean the fix is small. `decision` means implementation must wait for an explicit
contract, product, or architecture choice.

Status vocabulary: `open`, `in_progress`, `fixed`, `accepted`, `superseded`.

## Critical

| ID | Knowledge | Status | Finding | Primary locations |
|---|---|---|---|---|
| C-K1 | known | fixed | Server-only TLS is incorrectly accepted as verified caller identity for broker controls. Require token/OIDC or verified mTLS client identity. | `cmd/mecak8s/serve.go` (`validateBrokerControlOwnership`) |
| C-K2 | known | fixed | `mecated` resolves explicit broker authority canonically and atomically mounts the complete broker handler bundle on its fully populated HTTP mux after all-route, standard-method collision preflight. | `cmd/mecated/main.go`; `internal/adapter/mcpbroker/handlers.go`; `cmd/mecated/mcp_broker_command_root_test.go` |
| C-D1 | decision | fixed | Production workspace enrollment is implemented by the concrete broker `Attachment`, backed by its owning ToolHive `Process`: begin/observe/cancel reuse broker authorization state, authenticated observation freezes the catalogue, and composition advertises the capability only when the process requires it. | `internal/adapter/mcpbroker/workspace_enrollment.go`; `internal/adapter/mcpbroker/toolhive_process.go`; `internal/app/build.go`; `internal/adapter/server/workspace_enrollment.go` |
| C-K3 | known | fixed | Operator/public docs now describe the supported Helm OAuth broker values, SecretKeyRef client-secret boundary, OIDC verified caller identity, browser/session authorization, public callback routing, and the unresolved process-local multi-replica limitation; they do not advertise removed credential/profile fields. | `docs/usage/mecak8s.md`; `docs/architecture.md`; `user-docs/building/deployment/mecak8s.md`; `deploy/helm/mecak8s/values.schema.json` |

## High

| ID | Knowledge | Status | Finding | Primary locations |
|---|---|---|---|---|
| H-K1 | known | fixed | `mecak8s` defaults configured operator MCP profiles to broker authority, preserves legacy `--mcp-server` as global, and the Helm zero-server render explicitly selects global without constructing broker resources. | `cmd/mecak8s/flags.go`; `internal/app/build.go`; `deploy/helm/mecak8s/templates/` |
| H-K2 | known | fixed | `mecatui` and `mecatequi` retain legacy MCP profile loading while routing operator MCP configuration through the canonical authority resolver; mecatui supports broker authority and mecatequi rejects broker mode explicitly. | `cmd/mecatui/main.go`; `cmd/mecatui/main_test.go`; `cmd/mecatequi/flags.go`; `cmd/mecatequi/mcp_summary_test.go` |
| H-K3 | known | fixed | HTTP authorization continuations remain bound to the control request context: cancellation wins through the serialized prepared-run handoff; it aborts and deregisters the inert run and restores the authorization claim before protected work can start. | `internal/adapter/server/http.go`; `internal/adapter/server/mcp_authorization.go`; `internal/adapter/server/mcp_authorization_lifecycle_test.go` |
| H-K4 | known | fixed | Already satisfied by the current `relayMCPAuthorizationControl`, contrary to the row: an initial status-send failure skips the recv goroutine, drains every subsequent event into `RunEventRecorder`, and finishes the run through the deferred `FinishRun`. Verified by `TestMCPAuthorizationGRPCInitialStatusSendFailureDrainsAndFinishesContinuation` (renamed from `...CancelsAndFinishes...`, since after H-K5 the path drains without cancelling). | `internal/adapter/server/grpc.go` (`relayMCPAuthorizationControl`) |
| H-K5 | known | fixed | A control stream is a one-shot RPC on a context deliberately detached from the continuation (`context.WithoutCancel`), so it is not the run's owner and a transport fault is not a cancellation. Removed `result.Run.Cancel()` from all three non-client-initiated sites (initial status-send failure, non-EOF `recv()` error, mid-stream `send()` failure); only an explicit `cancel` frame still cancels. ONE deliberate exception found by testing and kept: a dead stream is also the only channel that can answer a permission ask, so a run parked on an ask (an ask being the most recent observed event) is cancelled rather than left wedged forever with its sole reader blocked — without it `TestMCPAuthorizationGRPCRejectsRepeatedInitialControl` hangs, and so would production. Guarded by `TestMCPAuthorizationGRPCNonEOFControlErrorDoesNotCancelContinuation`, which holds the resumed protected call in flight and fails if the run context is cancelled (mutation-verified against the old code). HTTP is deliberately NOT changed: H-K3 established the SSE control as request-bound by design, so the two transports differ on purpose. | `internal/adapter/server/grpc.go` |
| H-K6 | known | fixed | Broker OAuth now rejects every non-zero `network` control at the authority boundary because ToolHive cannot enforce the configured exact-origin policy; it still requires an explicit empty `network: {}`. Global profiles retain the strict policy unchanged. Table-driven tests cover `additional_origins`, `private_origins`, and `max_redirects` for OIDC and explicit OAuth2 before broker construction. | `internal/cliconfig/mcp_authority.go` (`validateBrokerOAuth`); `internal/cliconfig/mcp_authority_test.go` (`TestMCPAuthorityBrokerRejectsUnsupportedNetworkControlsBeforeConstruction`); `docs/usage/configuration.md` |
| H-K7 | known | fixed | `HandlerBundle.Mount` now preflights every fixed/callback route across standard methods before any registration; command-root tests prove method-qualified collisions leave no partial mount. | `internal/adapter/mcpbroker/handlers.go`; `cmd/mecated/mcp_broker_command_root_test.go` |
| H-K8 | known | fixed | Enrollment observation and cancellation require the returned broker ref to equal the exact pending ref (ID, required-service count, and instant); a valid result for another enrollment cannot mutate the aggregate. | `internal/adapter/server/workspace_enrollment.go` |
| H-K9 | known | fixed | Connected enrollment uses the observed frozen catalogue as the single snapshot for both executable engine wrappers and persisted authority names; it never re-reads a potentially advanced attachment catalogue during rebuild. | `internal/adapter/server/workspace_enrollment.go`; `internal/adapter/server/service.go` |
| H-K10 | known | fixed | Loss of the exact broker incarnation clears and persists the pre-prompt gate as a failed enrollment, allowing a later connect/retry to begin a replacement rather than leaving the session permanently wedged. Binding mismatch is explicitly classified as unavailable state. | `internal/adapter/server/workspace_enrollment.go`; `internal/adapter/server/mcp_broker.go` |
| H-K11 | known | fixed | Enrollment compensation detaches from the failed request but is bounded by the shared broker cleanup deadline, so a stuck cancellation cannot wedge session creation/control indefinitely. | `internal/adapter/server/workspace_enrollment.go` |
| H-K12 | known | fixed | TUI workspace enrollment is discoverable and on-demand through capability/controller-gated `/tools-connect` and `/tools-cancel`; the prompt remains usable, gate rejections point to the command, and queued initial or rejected typed prompts resubmit once after a connected control response. | `cmd/mecatui/ui/workspace_enrollment.go`; `cmd/mecatui/ui/builtins.go`; `cmd/mecatui/ui/update.go` |
| H-K13 | known | fixed | Pending workspace enrollment and per-tool MCP browser authorization poll their existing observation/control APIs every three seconds only after browser presentation. Polls are correlated by session/enrollment or session/authorization/control-generation, reject stale ticks/results, never overlap, and are cancelled on reset, cancellation, terminal resolution, or supersession. Failed enrollment remains an explicit `/tools-connect` retry; presentation URLs remain ephemeral. | `cmd/mecatui/ui/workspace_enrollment.go`; `cmd/mecatui/ui/mcp_authorization.go` |
| H-K14 | known | fixed | HTTP now has session-affinity-protected unary workspace-enrollment connect, retry, and cancel controls. They require empty bodies, validate path correlation, delegate to the shared service with the request context, use the established error mapping, and project only the existing safe enrollment fields; no HTTP-specific continuation/state machine or callback/selector disclosure was added. Focused tests cover observation, exact retry, stale/malformed cancellation, foreign owners, body rejection, and affinity inventory. | `internal/adapter/server/http.go`; `internal/adapter/server/workspace_enrollment_test.go`; `internal/adapter/server/http_affinity_test.go` |
| H-K15 | known | fixed | Chart-owned permission configuration can no longer silently override operator `--permission-config`; the Helm render preserves `extraArgs` before the chart-owned MCP settings path, so first-file-wins folding selects the operator configuration. | `deploy/helm/mecak8s/templates/deployment.yaml`; `deploy/helm/mecak8s/chart_test.go` |
| H-K16 | known | fixed | Living architecture describes the public broker authorization controls and configured HTTPS callback lifecycle. | `docs/architecture.md` |
| H-K17 | known | open | Broker deletion can succeed before durable session deletion fails, leaving an unrecoverable stored session. | `internal/adapter/server/service.go` |
| H-K18 | known | fixed | `authorization.required`/`authorization.resolved` are client-visible — `cmd/mecatui/client/msgs.go` decodes them into `MCPAuthorizationMsg`, the sole client signal that pops the MCP-authorization modal. Reverted the public-event filter that dropped them; they now reach the client wire again. | `cmd/mecatui/client/msgs.go`; `internal/adapter/server/grpc.go` |
| H-K19 | known | fixed | `757fe3d81` conflated binding-mismatch errors with genuine broker-unavailable errors under `brokercontract.ErrStateUnavailable`; `RecheckMCPAuthorization` silently redirected a mismatch to `AuthorizationInterrupted` instead of hard-failing. Needs a distinct sentinel or a check before the shared `errors.Is` branch. Not yet fixed. **FIXED:** binding mismatch now also carries the distinct `server.ErrBrokerBindingMismatch` sentinel, and the live authorization controls test through the new `brokerStateLost` predicate (`ErrStateUnavailable` AND NOT mismatch) instead of a bare `errors.Is`. Repair/settlement paths (`interruptRestoredAuthorizationLocked`, `settleAuthorizationLocked`) deliberately keep the raw `ErrStateUnavailable` check — a mismatch there genuinely is lost state. `TestMCPAuthorizationBindingMismatchDoesNotInterrupt` was indeed failing and now passes. | `internal/adapter/server/mcp_broker.go`; `internal/adapter/server/mcp_authorization.go` |
| H-K20 | known | fixed | Both `DEBUGTRACE fmt.Fprintf(os.Stderr, ...)` lines removed from `MCPAuthorizationPresentation`, along with the now-unused `os` import. At the time, no other `DEBUGTRACE` line anywhere in the tree was touched by this fix; the rest (`grpc.go`, `service.go`, `ownership.go`, `client.go`, `login.go`, `build.go`, `toolhive_process.go`, `auth.go`, and the whole temporary `debugTraceHTTPMiddleware` in `cmd/mecak8s/serve.go` — see H-K28) was removed in a separate later pass once every symptom it was tracing had a real diagnosis (H-K26/H-K27/H-K29). | `internal/adapter/server/mcp_authorization.go` |
| H-K21 | known | fixed | Live repro (logs) resolved: the GitHub OAuth round trip always completed correctly (302→303, then originally a 204 No Content) — an isolated A/B test of `openBrowserURL`'s `open -b com.google.Chrome` path against a plain non-mecatl redirect confirmed the browser launcher is NOT at fault. The real bug was the blank 204 success page itself: indistinguishable from a frozen browser, so a completed authorization got closed as "stuck," and the later 400 ("mcp broker authorization not found") was a self-inflicted retry against the already-consumed one-shot link — a UX gap, not a functional bug. `CallbackHandler`'s success path now returns a real "Authorization complete — you can close this window" HTML page instead of 204. **LIVE-VERIFIED for real** after H-K26 was found and fixed: fresh session, full OAuth round trip, server log shows `CallbackHandler SUCCESS` → `HTTP OUT method=GET path=/mcp/authorization/callback status=200` (not 204). | `internal/adapter/mcpbroker/auth.go` (`CallbackHandler`) |
| H-K22 | known | fixed | `NotFound` from the three MCP-authorization RPCs is a MISLEADING message, not a missing session. `MCPAuthorizationPresentation`/`RecheckMCPAuthorization`/`CancelMCPAuthorization` discard the real reason and `return ErrNotFound` bare at four distinct gates each (absent/foreign session; no matching pending authorization; expired `ExpiresAt`; status != pending), all surfacing to the user as `rpc error: code = NotFound desc = server: session not found`. Diagnostic value: the BARE form (no `: "id"` suffix) is itself the tell — `GetSession` always wraps as `%w: %q`, so an unsuffixed message proves the failure came from one of these bare gates, not from session lookup. Live evidence: session `32b16f86d623517da9a35e162421edd8` was present in Redis, correctly owned, and had zero `ownsResource MISMATCH` log lines. Distinct reasons need distinct sentinels (or at minimum distinguishable messages); "session not found" for an expired/absent pending authorization sends every future debugger to the wrong subsystem, as it did here. **FIXED:** each gate now returns a named `ErrNotFound`-wrapping error naming the actual reason (`errAuthorizationSessionUnavailable` / `errAuthorizationNoPending` / `errAuthorizationExpired` / `errAuthorizationNotPending` / `errAuthorizationUnclaimable`). No new wire taxonomy: they all stay `errors.Is(_, ErrNotFound)` so the gRPC/HTTP codes are unchanged. The session gate keeps ONE message for both absent and foreign sessions so ownership stays undisclosed; the expired check in `MCPAuthorizationPresentation` was split out of its combined error-or-expired condition to become separately reportable. | `internal/adapter/server/mcp_authorization.go` (`MCPAuthorizationPresentation`, `RecheckMCPAuthorization`, `CancelMCPAuthorization`, `loadMatchingAuthorization`) |
| H-K23 | known | open | A successfully parked external authorization does not survive cancellation of the run that parked it, which is precisely the window the browser flow occupies. `parkAuthorization` is internally correct — it persists `StateAuthorizing` before emitting `authorization.required`, and every pre-emit failure rolls back — so once the modal is on screen the park is durable w.r.t. that function. But `Session.PendingAuthorization()` returns `(_, false)` unless `State == StateAuthorizing`, so any subsequent transition to `cancelled` clears the park while the user is still in the browser; the next control then hits H-K22's bare `ErrNotFound`. Live evidence: `32b16f86d623517da9a35e162421edd8` persisted `state:"cancelled"`, `stop_reason:"cancelled"`, `Turns:1 ToolCalls:1 ConsecutiveFailures:1`, NO `pending_authorization` field, and a recorded tool result of `mcpbroker: connect protected ToolHive target: mcp: connect to server "broker": context canceled` — i.e. the run's context died mid-flow and took the park with it. Cancellation mechanism now settled BY CODE INSPECTION: the "continuation run ctx is bound to the control RPC ctx" theory is REFUTED — `RecheckMcpAuthorization`/`CancelMcpAuthorization` detach with `runCtx := context.WithoutCancel(stream.Context())` before calling the Service, so stream teardown does not reach the run through ctx inheritance. The same symptom is instead produced EXPLICITLY by `relayMCPAuthorizationControl`, which retains `stream.Context()` and calls `result.Run.Cancel()` at three non-client-initiated sites: initial status-send failure, a NON-EOF `recv()` error (= H-K5, still open), and a mid-stream `send()` failure. `Run.Cancel()` cancels the run ctx, which is the parent of tool execution, so an in-flight `mcpadapter.Connect` in `toolHiveProtectedCaller` returns exactly the recorded `context canceled`. Chain is therefore complete and code-only: control-stream drop → `Run.Cancel()` → connect fails → run cancelled → session leaves `StateAuthorizing` → park cleared → next control returns H-K22's bare `ErrNotFound`. STILL open: what dropped the stream (client Ctrl-C vs transport fault) — the only part needing a live repro. No fix attempted. This was ORIGINALLY diagnosed against session `32b16f86…` under the mistaken belief that the deployed image was current (it was not — see H-K26); the mechanism itself is sound (derived from code + a real persisted snapshot), but confirm it against a session created AFTER a verified-correct deploy before treating it as the live-reproduced explanation for any NEW "session not found" report. **Fix landed via H-K5, live confirmation still owed.** With the control stream no longer cancelling the continuation, the identified chain is broken at its first link and the offline regression test proves the in-flight continuation survives a control-stream transport fault. The remaining step — a live second-protected-call-in-the-same-session round trip after a verified deploy — needs an interactive TUI and a human GitHub OAuth consent, so it was not executed here. | `engine/agent/loop.go` (`parkAuthorization`, `rollbackAuthorization`); `engine/session/authorization.go` (`PendingAuthorization`); `internal/adapter/mcpbroker/toolhive_process.go` (`toolHiveProtectedCaller`) |
| H-K24 | known | superseded | Correctly identified that H-K21's fix was absent from the running deployment and that this (not a new bug) produced the "browser never redirects" symptom — but its stated reason (an intermediate/stale BUILD) was wrong; see H-K26 for the actual mechanism (deployment pinned to an unmanaged image tag). | `internal/adapter/mcpbroker/auth.go` (`CallbackHandler`); running image `ko.local/mecak8s:dev` |
| H-K25 | known | superseded | Correctly proved, by elimination over the code, that current source COULD NOT emit a 204 — but concluded from that the fix must be uncommitted-and-lost across "~100 worktrees," which was wrong: the fix was on disk in the right worktree the whole time (confirmed directly: the build's own dirty-file list named `auth.go`, and `strings` on the extracted image layer found the literal "Authorization complete" text). The real reason the elimination-by-code and the live 204 disagreed is H-K26 — the running Deployment was never pointed at the image being rebuilt. The recommended countermeasure ("verify the artifact, not a log line") is sound and is exactly what settled H-K26; keep using it. | `internal/adapter/mcpbroker/auth.go` (`CallbackHandler`) |
| H-K26 | known | fixed | **The actual root cause of the entire "is my fix live" confusion tonight** (H-K24, H-K25, and the earlier unexplained Redis-auth-storage `DEBUGTRACE` silence all trace back to this): the `mecak8s-mecak8s` Deployment's pod spec had `image: ko.local/mecak8s:debugtrace` with `imagePullPolicy: IfNotPresent` — a one-off tag from earlier ad-hoc debug-instrumentation work, NOT the `:dev` tag every rebuild tonight (`task image-build-load`'s internal cmds, and the manual `ko build --tags=dev` workaround) actually targeted. `IfNotPresent` means kubelet never re-checks; every `kubectl rollout restart` faithfully recreated the pod from the exact same stale `:debugtrace` image, regardless of how many times `:dev` changed. The properly-managed path (`task chart-apply`, via `helm upgrade` + `deploy/helm/mecak8s/values-kind.yaml`) has always declared `tag: dev` correctly — the drift was from an unmanaged, unrecorded `kubectl set image`/`helm upgrade --set` done outside that path, with no `--record`/change-cause left behind to show when or by whom. Fixed live via `kubectl set image deployment/mecak8s-mecak8s agent=ko.local/mecak8s:dev`, verified by comparing the new pod's `status.containerStatuses[0].imageID` against the independently-extracted image's digest (match), then a full live OAuth round trip confirmed `status=200` (see H-K21). The manual `kubectl set image` fix itself is unmanaged (matches the chart's declared value but isn't reconciled through Helm) — `task chart-apply` should be run once to bring the Helm release's recorded state back in line. | Deployment `mecak8s-mecak8s` pod spec (image field); `deploy/helm/mecak8s/values-kind.yaml` (`image.tag: dev`, the correct declared value) |
| H-K27 | known | fixed | **Redis-backed embedded-auth-server storage: LIVE-VERIFIED working, and the earlier "zero Redis writes" result is now explained as an H-K26 artifact.** Rebuilt/loaded/pointed the deployment at `ko.local/mecak8s:recon-auth-stack` (distinct from the acc branch's `:dev`) and verified the artifact per H-K25/H-K26 discipline — pod `status.containerStatuses[0].imageID` matched `docker inspect --format {{.Id}}` of the just-built image. Boot log then showed `DEBUGTRACE toolhive_process authStorage: upstreams=1 AuthStorage!=nil=false AuthRedisClient!=nil=true` → `selected REDIS prefix="mecatl:authserver:"` (never `selected MEMORY (fallback)`). Live proof of the write path AND of cross-process resolution, in four steps: (1) parked a real `mcp__github__get_me` call (`authorization.required`), fetched the presented URL, and hit the broker's `/v1/mcp/broker/oauth/authorize` — this wrote exactly one new key, `mecatl:authserver:pending:BKWXZJP6JWPFQ25AEP2V2WI2GU`, whose suffix IS the `state` the 302 to `github.com/login/oauth/authorize` carries, so the callback lookup is keyed directly on that state; (2) CONTROL: callback with a state that never existed → `HTTP 400 authorization request not found or expired`; (3) `kubectl delete pod` mid-flow, new pod up, pending key still `EXISTS 1` with TTL 513s and the new pod again logged `selected REDIS`; (4) callback with the REAL state on the NEW pod → `HTTP 303`, NOT the control's 400, and the log shows it proceeded past the lookup all the way to the upstream exchange: `failed to exchange code or resolve identity ... bad_verification_code - The code passed is incorrect or expired` (the code was deliberately bogus), after which the key was consumed (`EXISTS 0`). Found-then-exchange vs never-found is the decisive discriminator, and it landed on the found side. SCOPE/HONESTY: a real GitHub `code` cannot be minted headlessly (that leg needs a browser with github.com cookies for this specific OAuth App), so step 4 exercised everything restart-sensitive — the state lookup in the restarted process — and stopped only at code validity, which is not restart-sensitive. NOT sufficient for end-to-end restart survival on its own: see H-K29 and the pre-existing M-D2. | `internal/app/mcp_broker_toolhive.go` (`buildToolHiveAuthRedisClient`); `internal/adapter/mcpbroker/toolhive_process.go` (auth-storage selection); `internal/adapter/mcpbroker/toolhive_construction.go` (`toolHiveAuthStoragePrefix`) |
| H-K28 | known | fixed | **A temporary debug middleware silently 500'd EVERY SSE endpoint in `cmd/mecak8s`.** `debugTraceHTTPMiddleware` wraps the `http.ResponseWriter` in `statusCapturingWriter` to capture the status for its `DEBUGTRACE HTTP OUT` line, but that type only embeds `http.ResponseWriter` and overrides `WriteHeader` — it does not implement `http.Flusher`. Every streaming handler in `internal/adapter/server/http.go` does `flusher, ok := w.(http.Flusher); if !ok { writeError(w, 500, "streaming unsupported") }` at seven sites, so with the middleware installed the type assertion failed for all of them: prompt SSE, the MCP-authorization recheck/cancel controls, and the live feed all returned `500 streaming unsupported` before doing any work. Hit while trying to drive the H-K27 test over HTTP; reproduced identically through the public ngrok domain and direct to the node port, which ruled out an edge-buffering explanation. Fixed at the time by giving `statusCapturingWriter` a `Flush()` that forwards to the wrapped writer when it is itself a Flusher. Worth noting for the same reason as H-K26: the failure was in throwaway diagnostic scaffolding, not the feature, and it presented as a plausible-looking infrastructure symptom ("ngrok buffers SSE") that would have sent a debugger to the wrong subsystem. **RESOLVED FOR REAL:** `debugTraceHTTPMiddleware`/`statusCapturingWriter` (Flush() included) has since been removed entirely along with this session's other `DEBUGTRACE` instrumentation (see H-K20, H-K30) — there is nothing left for this bug to live in. | `cmd/mecak8s/serve.go` (removed); assertion sites in `internal/adapter/server/http.go` (unaffected once the middleware is gone) |
| H-K29 | known | open | **Redis auth storage is necessary but NOT sufficient: a parked session still cannot resume after a restart, because the broker binding is random per process.** With H-K27's pending record provably surviving, the same restarted pod still rejects every control on the session that parked: `POST /v1/sessions/{id}/mcp-authorizations/{aid}/recheck` → `HTTP 412 ... MCP broker binding mismatch for session "6f56b04fa98dc203287b840e1ccb46cd"`, and a fresh prompt on that session fails the same way. Cause is direct and code-only: `mcpbroker.New` does `bindingSeed := make([]byte, 18); rand.Read(bindingSeed)` and sets `bindingPrefix` from it on every Runtime construction, `Attachment.Binding()` returns `bindingPrefix + "." + generation`, and `openBrokerAttachment` hard-fails when the session's persisted `ExternalBinding` differs. No Option seeds `bindingPrefix` (grepped: the only references are its declaration, that assignment, and the read in `Binding()`), so a restart necessarily invalidates every session's binding. Two further per-process pieces compound it: `Runtime.states` is an in-memory `map[string]callbackState` holding the mecatl-side callback correlation (distinct from the ToolHive auth server's now-Redis-backed upstream record — note the two callbacks are different endpoints, `/mcp/authorization/callback` vs `/v1/mcp/broker/oauth/callback`), and the embedded auth server logs `no HMAC secrets configured, generating ephemeral secret` + `generated ephemeral signing key - tokens will be invalid after restart` (ES256) at every boot. So the Redis work correctly fixes the upstream-facing leg it targets, while the stated user-visible goal in `buildToolHiveAuthRedisClient`'s own comment ("pending OAuth authorizations survive a pod restart") is not yet achieved end to end. This is live evidence for the already-open M-D2 rather than a new decision — closing it needs a stable binding seed plus configured HMAC/signing material, not more storage backing. | `internal/adapter/mcpbroker/runtime.go` (`New` binding seed, `Attachment.Binding`, `states`); `internal/adapter/server/mcp_broker.go` (`openBrokerAttachment`, `ErrBrokerBindingMismatch`); see M-D2 |
| H-K30 | known | fixed | **HEAD did not build.** `8d64f2ec1` (the H-K5/H-K19/H-K20/H-K22/H-K23 fix commit) added `Service.beforeAuthorizationContinuationStart`'s two call sites in `mcp_authorization.go` and its usage in `mcp_authorization_lifecycle_test.go`, but never committed the field declaration itself on the `Service` struct — a straight compile failure at that commit, only masked because the field addition happened to still be sitting uncommitted in the working tree when this was noticed. Restored the field declaration in the same pass that removed this session's `DEBUGTRACE` instrumentation and reverted the `openBrowserURL` Chrome-launcher change (ruled out by H-K21/H-K24 as never having been the actual cause of the redirect symptom). | `internal/adapter/server/service.go` (`Service.beforeAuthorizationContinuationStart`) |
| H-K31 | known | open | Investigate the embedded vMCP incoming-authentication token lifecycle: issuer/audience binding, access-token expiry, refresh or remint path, revocation, and behavior when the harness restarts while the broker remains available. | `internal/adapter/mcpbroker/toolhive_process.go`; `internal/adapter/mcpbroker/auth.go`; `internal/adapter/mcpbroker/runtime.go` |

| H-D1 | decision | open | Multiple OAuth upstreams are rejected. Define bundle identity, anchor selection, callback correlation, and grant scope. | `internal/cliconfig/mcp_authority.go`; `internal/adapter/mcpbroker/toolhive_construction.go` |
| H-D2 | decision | open | Recent lazy static protected-tool behavior conflicts with ADR 0291. Choose and document the authoritative contract. | source ADR 0291; `internal/adapter/mcpbroker/toolhive_construction.go` |
| H-D3 | decision | resolved | Failed preparation or registration restores the exact claimed continuation through `Session.RestoreAuthorizationClaim`, persists `authorizing`, and leaves the existing expiry timer armed; the claim is consumed only after the prepared run is registered. | `engine/session/authorization.go`; `internal/adapter/server/mcp_authorization.go` |
| H-D4 | decision | open | A nominally remote-ready broker boundary returns executable `tool.Tool` values. Decide local-wrapper versus data-oriented remote protocol. | `internal/mcpbroker/broker.go` |
| H-D5 | decision | open | `app.Build` hard-codes and exports the concrete in-process broker. Define remote broker injection/ownership. | `internal/app/build.go` |
| H-D6 | decision | open | Ambiguous `Attachment.Commit` failure has no recoverable creation operation for a remote broker. | `internal/adapter/server/service.go`; `internal/mcpbroker/broker.go` |
| H-D7 | decision | open | Helm defaults to two replicas while broker state is process-local. Choose single replica, affinity limitation, or durable broker requirement. | `deploy/helm/mecak8s/values.yaml` |
| H-D8 | decision | open | No frozen ADR covers the final state, ownership, API, and restart decisions. Write it after the preceding contract decisions settle. | `docs/adr/` |

## Medium

| ID | Knowledge | Status | Finding | Primary locations |
|---|---|---|---|---|
| M-K1 | known | fixed | Valid absolute HTTPS callback URLs may omit a path or use `/`; both normalize to `/`, which mounts as exact GET `/{$}` without claiming fallback-owned subpaths, while collision preflight remains method-aware. | `internal/cliconfig/mcp_authority.go`; `internal/adapter/mcpbroker/handlers.go` |
| M-K2 | known | fixed | `app.Build` rejects broker authority plus programmatic `MCPServers` after resolving effective authority (including a loader result), before MCP, broker-process, or Redis construction. | `internal/app/build.go`; `internal/app/mcp_broker_p10_test.go` |
| M-K3 | known | fixed | Continuation preparation accepts only constructor-validated `session.AuthorizationResolution` values and returns an error for zero, pending, or unknown outcomes before constructing a run or mutating the session. | `engine/session/authorization.go`; `engine/agent/loop.go`; `internal/adapter/server/mcp_authorization.go` |
| M-K4 | known | fixed | Enrolled tool sets are limited to 256 unique valid UTF-8 names and 32 KiB aggregate name bytes; session completion stays atomic, and the sealed catalogue constructor short-circuits before retaining an over-limit entry. | `engine/session/workspace_enrollment.go`; `internal/mcpbroker/broker.go` |
| M-K5 | known | open | Restored parked authorizations do not regain deterministic expiry settlement. | `internal/adapter/server/service.go` |
| M-K6 | known | open | gRPC control send errors are drained but returned as nil. | `internal/adapter/server/grpc.go` |
| M-K7 | known | fixed | `request_refresh_token` is forwarded through the app-to-ToolHive construction boundary and emitted as ToolHive's offline authorization parameter for both OIDC and explicit OAuth2 upstreams. | `internal/app/mcp_broker_toolhive.go` (`toolHiveBrokerConfig`); `internal/app/mcp_broker_toolhive_test.go` (`TestToolHiveBrokerConfigProjectsRefreshTokenRequest`); `internal/adapter/mcpbroker/toolhive_process_test.go` (`TestToolHiveConstructionMapsRefreshTokenRequest`) |
| M-K8 | known | fixed | Workspace-enrollment browser-launch failures are correlated to the active session, enrollment, and control generation, then shown as sanitized user-visible errors. | `cmd/mecatui/ui/workspace_enrollment.go` |
| M-K9 | known | fixed | Workspace enrollment surfaces sanitized authoritative RPC errors instead of replacing them with a generic failure where an authoritative cause exists. | `cmd/mecatui/ui/workspace_enrollment.go` |
| M-K10 | known | fixed | MCP authorization control readers use their owning control-generation context, not the broad application context; reset/supersession cancellation therefore stops the reader. | `cmd/mecatui/ui/mcp_authorization.go` |
| M-K11 | known | open | Typed-prompt recovery and exact-once text-only resubmission are absent. | `cmd/mecatui/ui/update.go` |
| M-K12 | known | fixed | Helm values/schema render the runtime `mcp.broker.callback_url` authority for exactly one OAuth profile, fail missing/invalid or unmatched callback metadata and missing OIDC caller identity early, and retain explicit global routing for zero servers. | `deploy/helm/mecak8s/values.yaml`; `deploy/helm/mecak8s/values.schema.json`; `deploy/helm/mecak8s/templates/` |
| M-K13 | known | fixed | Public mecak8s documentation covers broker callback values, public HTTPS reachability, SecretKeyRef boundaries, and global zero-server routing. | `user-docs/building/deployment/mecak8s.md`; `docs/usage/mecak8s.md` |
| M-K14 | known | open | Three server paths independently reconcile `CancelOutcome`; extract one private helper. | `internal/adapter/server/mcp_authorization.go` |
| M-K15 | known | fixed | Process-external session bindings use the named opaque `session.ExternalBinding` type end-to-end across the aggregate, snapshot, broker attachment contract, implementation, and server reattachment checks; the distinct per-authorization `AuthorizationBinding` remains non-interchangeable. | `engine/session/session.go`; `engine/adapter/sessnap/sessnap.go`; `internal/mcpbroker/broker.go`; `internal/adapter/mcpbroker/runtime.go`; `internal/adapter/server/mcp_broker.go` |
| M-D1 | decision | open | Decide whether `mcpauthority` is intentionally a concrete composition DTO or a neutral boundary. | `internal/adapter/mcpauthority/authority.go` |
| M-D2 | decision | open | Process-local broker state cannot reattach after restart. Keep deterministic settlement unless durable/remote scope is selected. | `internal/adapter/mcpbroker/runtime.go` |
| M-D3 | decision | open | Public enrollment proto/client controls precede authoritative server behavior in the patch series. Reorder or accept the reviewability trade-off. | commits `09af052b5`, `02dfeb853` |

## Low

| ID | Knowledge | Status | Finding | Primary locations |
|---|---|---|---|---|
| L-K1 | known | open | Align gRPC control error-return behavior with the Converse relay; covered by M-K6. | `internal/adapter/server/grpc.go` |
| L-D1 | decision | open | Define whether “frozen catalogue” promises immutable metadata/membership only or immutable executable behavior. | `internal/mcpbroker/broker.go` |

## Excluded panel reports

- The reported generic OAuth2 pointer-decoding defect was a false positive;
  `newPermconfigNodePointer(&u.OAuth2)` is already present.
- Process-local broker restart loss is not treated as a hidden regression under
  the accepted in-process-only scope; deterministic settlement remains M-D2.
- The library-reuse pass found no actionable reductions.
- Apparent duplication was rejected except for M-K14.
