# Agent model discovery orchestration

- **Acceptance plan:** `docs/acceptance/agent-model-discovery.md`
- **Accumulator:** `acc/agent-model-discovery`
- **Scope:** one focused implementation task for bounded, read-only model discovery over the existing resolved inventory.

The task state in `tasks/` is managed by `/plan-orchestrate`. The acceptance plan and its cited ADRs remain the behavioral contract.
