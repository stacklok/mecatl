# Listener-scoped workspace authority

Accumulator: `acc/listener-scoped-workspace-authority`

The task graph applies one Service-owned authority policy at every filesystem-bearing session creation or restoration path, then wires listener composition and clients around it. The final task updates the operator-facing documentation and validates the assembled behavior.
