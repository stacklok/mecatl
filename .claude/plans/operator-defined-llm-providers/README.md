# Operator-defined LLM providers plan

Accumulator: `acc/operator-defined-llm-providers`

The acceptance contract is `docs/acceptance/operator-defined-llm-providers.md`.
Tasks are intentionally sequenced: strict operator configuration and credential bootstrap
first, then registry construction, live inventory, and command-root integration.
