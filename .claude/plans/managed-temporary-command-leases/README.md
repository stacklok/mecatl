# Managed temporary command leases task graph

Accumulator: `acc/managed-temporary-command-leases`

The tasks implement `docs/acceptance/managed-temporary-command-leases.md` in dependency order. Task frontmatter is orchestration state; git ancestry is authoritative.
